[pytest]
# asyncio_mode=auto means every async test function is treated as an async
# test without needing the @pytest.mark.asyncio decorator on each one.
# Set to "strict" if you prefer explicit marking.
asyncio_mode = auto

testpaths = tests
