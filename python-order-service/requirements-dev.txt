# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.5
pytest-asyncio==0.25.3   # async test support (asyncio_mode=auto in pytest.ini)
httpx==0.28.1             # async HTTP client used by test_producer.py
